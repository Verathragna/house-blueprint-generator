version: '3.8'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEVICE=cpu
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    depends_on:
      - app
